# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Dependencies & builds
node_modules/
dist/
build/
.next/
__pycache__/
*.pyc
venv/
.venv/

# Environment / secrets
.env
.env.*
!.env.example

# Ralph loop runtime output
ralph/logs/
ralph/.state/
*.log

# API keys - NEVER commit
SECRET/
*.env

# Fujikin's own documents - the brief, the character manual and the official
# artwork. Not ours to publish on a public repo.
source-materials/
Documents_for_dev/

# Downloaded font masters used only when rebuilding the offline HTML
tools/_fontcache/

# Local safety copies taken before risky changes - never publish
_backup/
